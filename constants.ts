
import { ProblemType, Complexity, PromptFormat } from './types';

export const PROBLEM_TYPE_OPTIONS: ProblemType[] = Object.values(ProblemType);
export const COMPLEXITY_OPTIONS: Complexity[] = Object.values(Complexity);
export const PROMPT_FORMAT_OPTIONS: PromptFormat[] = Object.values(PromptFormat);
